<!DOCTYPE html>
<html>
  <head>
    <title>Chess</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="utf-8">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">

    <script src="https://use.fontawesome.com/7599d4c6a9.js"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

    <nav class="navbar navbar-expand-xl bg-light"> 
      <div class="container">

        <%= link_to 'Chess', root_path, class: 'navbar-brand' %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#content" aria-controls="content" aria-expanded="false" aria-label="Toggle navigation">
          <!-- <span class="navbar-toggler-icon"></span> -->
          <span><i class="fa fa-bars"></i></span>
        </button>

        <div class="collapse navbar-collapse" id="content">

          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if user_signed_in? && current_user.account%>
              <li class="nav-item">
                <%= link_to current_user.username, accounts_path, class: "nav-link" %>
              </li>
            <% end %>
            <li class="nav-item"> 
              <a class="nav-link" href="#">some1</a>
            </li>
            <li class="nav-item"> 
              <a class="nav-link" href="#">some2</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">some3</a>
            </li> 
          </ul>

          <!-- Devise: ссылки входа и выхода -->
          <div class="d-flex header_sign">
            <% if user_signed_in? %>
              <div class="me-3">
                <%= current_user.username %> 
              </div>
              <div>
                <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
              </div>     
            <% else %>
              <%= link_to "Sign In", new_user_session_path %>
            <% end %>
          </div>

        </div>

      </div>
    </nav>

    <!-- Devise-сообщения -->
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>


    <div class="container">
      <%= yield %>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
  </body>
</html>
